# Check Index Tests

This file was generated from existing `@lat` test backlinks so bootstrap output can preserve current test specifications.

## Detects missing index file

This generated test specification preserves the existing `@lat` backlink target `check-index#Detects missing index file`.

## Detects missing subdirectory index file

This generated test specification preserves the existing `@lat` backlink target `check-index#Detects missing subdirectory index file`.

## Detects non-markdown file

This generated test specification preserves the existing `@lat` backlink target `check-index#Detects non-markdown file`.

## Detects stale index entry

This generated test specification preserves the existing `@lat` backlink target `check-index#Detects stale index entry`.

## Detects stale subdirectory index entry

This generated test specification preserves the existing `@lat` backlink target `check-index#Detects stale subdirectory index entry`.

## Non-markdown files excluded from index listing

This generated test specification preserves the existing `@lat` backlink target `check-index#Non-markdown files excluded from index listing`.

## Passes with valid index

This generated test specification preserves the existing `@lat` backlink target `check-index#Passes with valid index`.

## Passes with valid subdirectory index

This generated test specification preserves the existing `@lat` backlink target `check-index#Passes with valid subdirectory index`.
