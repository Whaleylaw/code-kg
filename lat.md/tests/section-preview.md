# Section Preview Tests

This file was generated from existing `@lat` test backlinks so bootstrap output can preserve current test specifications.

## Formats section with firstParagraph

This generated test specification preserves the existing `@lat` backlink target `section-preview#Formats section with firstParagraph`.

## Formats section without firstParagraph

This generated test specification preserves the existing `@lat` backlink target `section-preview#Formats section without firstParagraph`.
