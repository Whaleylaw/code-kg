# Ts Fallback Tests

This file was generated from existing `@lat` test backlinks so bootstrap output can preserve current test specifications.

## checkCodeRefs detects dangling ref without rg

This generated test specification preserves the existing `@lat` backlink target `tests/ts-fallback#checkCodeRefs detects dangling ref without rg`.

## findRefs with code scope works without rg

This generated test specification preserves the existing `@lat` backlink target `tests/ts-fallback#findRefs with code scope works without rg`.

## getSection includes code back-refs without rg

This generated test specification preserves the existing `@lat` backlink target `tests/ts-fallback#getSection includes code back-refs without rg`.

## gitignore filtering works without rg

This generated test specification preserves the existing `@lat` backlink target `tests/ts-fallback#gitignore filtering works without rg`.

## scanCodeRefs finds refs without rg

This generated test specification preserves the existing `@lat` backlink target `tests/ts-fallback#scanCodeRefs finds refs without rg`.
