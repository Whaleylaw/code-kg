# Ref Resolution Tests

This file was generated from existing `@lat` test backlinks so bootstrap output can preserve current test specifications.

## Ambiguous short ref in code

This generated test specification preserves the existing `@lat` backlink target `ref-resolution#Ambiguous short ref in code`.

## Ambiguous short ref in md

This generated test specification preserves the existing `@lat` backlink target `ref-resolution#Ambiguous short ref in md`.

## Ambiguous short ref unique section

This generated test specification preserves the existing `@lat` backlink target `ref-resolution#Ambiguous short ref unique section`.

## Bare heading in code is error

This generated test specification preserves the existing `@lat` backlink target `ref-resolution#Bare heading in code is error`.

## Bare heading in md is error

This generated test specification preserves the existing `@lat` backlink target `ref-resolution#Bare heading in md is error`.

## Full ref findSections resolves

This generated test specification preserves the existing `@lat` backlink target `ref-resolution#Full ref findSections resolves`.

## Full ref passes check code-refs

This generated test specification preserves the existing `@lat` backlink target `ref-resolution#Full ref passes check code-refs`.

## Full ref passes check md

This generated test specification preserves the existing `@lat` backlink target `ref-resolution#Full ref passes check md`.

## Full ref refs finds code references

This generated test specification preserves the existing `@lat` backlink target `ref-resolution#Full ref refs finds code references`.

## Full ref refs finds md references

This generated test specification preserves the existing `@lat` backlink target `ref-resolution#Full ref refs finds md references`.

## Local section syntax in md is error

This generated test specification preserves the existing `@lat` backlink target `ref-resolution#Local section syntax in md is error`.

## Nested in-file refs pass

This generated test specification preserves the existing `@lat` backlink target `ref-resolution#Nested in-file refs pass`.

## Nonexistent file ref in md is error

This generated test specification preserves the existing `@lat` backlink target `ref-resolution#Nonexistent file ref in md is error`.

## Nonexistent leaf in nested ref is error

This generated test specification preserves the existing `@lat` backlink target `ref-resolution#Nonexistent leaf in nested ref is error`.

## Short ref findSections resolves

This generated test specification preserves the existing `@lat` backlink target `ref-resolution#Short ref findSections resolves`.

## Short ref passes check code-refs

This generated test specification preserves the existing `@lat` backlink target `ref-resolution#Short ref passes check code-refs`.

## Short ref passes check md

This generated test specification preserves the existing `@lat` backlink target `ref-resolution#Short ref passes check md`.

## Short ref refs finds code references

This generated test specification preserves the existing `@lat` backlink target `ref-resolution#Short ref refs finds code references`.

## Short ref refs finds md references

This generated test specification preserves the existing `@lat` backlink target `ref-resolution#Short ref refs finds md references`.

## Skipped intermediate in ref is error

This generated test specification preserves the existing `@lat` backlink target `ref-resolution#Skipped intermediate in ref is error`.

## Valid code ref with file prefix passes

This generated test specification preserves the existing `@lat` backlink target `ref-resolution#Valid code ref with file prefix passes`.

## Wrong nesting order in ref is error

This generated test specification preserves the existing `@lat` backlink target `ref-resolution#Wrong nesting order in ref is error`.
