# Check Sections Tests

This file was generated from existing `@lat` test backlinks so bootstrap output can preserve current test specifications.

## Detects missing leading paragraph

This generated test specification preserves the existing `@lat` backlink target `check-sections#Detects missing leading paragraph`.

## Detects overly long leading paragraph

This generated test specification preserves the existing `@lat` backlink target `check-sections#Detects overly long leading paragraph`.

## Excludes wiki link content from character count

This generated test specification preserves the existing `@lat` backlink target `check-sections#Excludes wiki link content from character count`.
