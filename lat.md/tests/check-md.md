# Check Md Tests

This file was generated from existing `@lat` test backlinks so bootstrap output can preserve current test specifications.

## Detects broken links

This generated test specification preserves the existing `@lat` backlink target `check-md#Detects broken links`.

## Passes with valid links

This generated test specification preserves the existing `@lat` backlink target `check-md#Passes with valid links`.

### Passes with C enum value links

This generated test specification preserves the existing `@lat` backlink target `tests/check-md#Passes with valid links#Passes with C enum value links`.

### Passes with C struct field links

This generated test specification preserves the existing `@lat` backlink target `tests/check-md#Passes with valid links#Passes with C struct field links`.
