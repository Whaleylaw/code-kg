# Expand Tests

This file was generated from existing `@lat` test backlinks so bootstrap output can preserve current test specifications.

## Passes through text without refs

This generated test specification preserves the existing `@lat` backlink target `tests/expand#Passes through text without refs`.

## Resolves exact ref with context

This generated test specification preserves the existing `@lat` backlink target `tests/expand#Resolves exact ref with context`.

## Resolves fuzzy ref with alternatives

This generated test specification preserves the existing `@lat` backlink target `tests/expand#Resolves fuzzy ref with alternatives`.
