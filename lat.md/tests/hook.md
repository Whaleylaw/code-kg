# Hook Tests

This file was generated from existing `@lat` test backlinks so bootstrap output can preserve current test specifications.

## Blocks when code diff is large but lat.md/ not updated

This generated test specification preserves the existing `@lat` backlink target `tests/hook#Blocks when code diff is large but lat.md/ not updated`.

## Blocks when lat check fails

This generated test specification preserves the existing `@lat` backlink target `tests/hook#Blocks when lat check fails`.

## Blocks with both messages when check fails and diff needs sync

This generated test specification preserves the existing `@lat` backlink target `tests/hook#Blocks with both messages when check fails and diff needs sync`.

## Cursor stop hook returns follow-up work instead of a Claude block

This generated test specification preserves the existing `@lat` backlink target `tests/hook#Cursor stop hook returns follow-up work instead of a Claude block`.

## Exits silently on second pass when check passes

This generated test specification preserves the existing `@lat` backlink target `tests/hook#Exits silently on second pass when check passes`.

## Exits silently when check passes and no diff

This generated test specification preserves the existing `@lat` backlink target `tests/hook#Exits silently when check passes and no diff`.

## Exits silently when code diff is below threshold

This generated test specification preserves the existing `@lat` backlink target `tests/hook#Exits silently when code diff is below threshold`.

## Exits silently when lat.md/ changes are proportional

This generated test specification preserves the existing `@lat` backlink target `tests/hook#Exits silently when lat.md/ changes are proportional`.

## Ignores non-code files in diff

This generated test specification preserves the existing `@lat` backlink target `tests/hook#Ignores non-code files in diff`.

## Prints stderr warning on second pass when check still fails

This generated test specification preserves the existing `@lat` backlink target `tests/hook#Prints stderr warning on second pass when check still fails`.
