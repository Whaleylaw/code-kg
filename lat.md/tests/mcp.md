# Mcp Tests

This file was generated from existing `@lat` test backlinks so bootstrap output can preserve current test specifications.

## lat_check reports errors

This generated test specification preserves the existing `@lat` backlink target `tests/mcp#lat_check reports errors`.

## lat_expand expands refs

This generated test specification preserves the existing `@lat` backlink target `tests/mcp#lat_expand expands refs`.

## lat_expand passes through text without refs

This generated test specification preserves the existing `@lat` backlink target `tests/mcp#lat_expand passes through text without refs`.

## lat_locate finds a section

This generated test specification preserves the existing `@lat` backlink target `tests/mcp#lat_locate finds a section`.

## lat_locate returns message for missing section

This generated test specification preserves the existing `@lat` backlink target `tests/mcp#lat_locate returns message for missing section`.

## lat_search finds auth section

This generated test specification preserves the existing `@lat` backlink target `tests/mcp#lat_search finds auth section`.

## lat_search finds performance section

This generated test specification preserves the existing `@lat` backlink target `tests/mcp#lat_search finds performance section`.

## lat_search returns no results message

This generated test specification preserves the existing `@lat` backlink target `tests/mcp#lat_search returns no results message`.

## lat_section returns message for missing section

This generated test specification preserves the existing `@lat` backlink target `tests/mcp#lat_section returns message for missing section`.

## lat_section shows section content

This generated test specification preserves the existing `@lat` backlink target `tests/mcp#lat_section shows section content`.

## Lists all tools

This generated test specification preserves the existing `@lat` backlink target `tests/mcp#Lists all tools`.
