# Ref Extraction Tests

This file was generated from existing `@lat` test backlinks so bootstrap output can preserve current test specifications.

## Extracts wiki link references

This generated test specification preserves the existing `@lat` backlink target `ref-extraction#Extracts wiki link references`.

## Returns empty for files without links

This generated test specification preserves the existing `@lat` backlink target `ref-extraction#Returns empty for files without links`.
